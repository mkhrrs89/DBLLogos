DBL Logos - Hall of Fame photo gallery update

Replace these three files in the root of your GitHub repository:
- index.html
- app.js
- styles.css

This adds a Details / Photo gallery switch inside the Hall of Fame tab.
The Details view remains the default. The Photo gallery displays only player images in a responsive grid and preserves each image's natural aspect ratio so nothing is cropped.
